const Failcomment = ({ comment }) => {
  return (
    <div
      style={{
        color: 'rgb(208, 57, 62)',
        fontSize: '13px',
        fontWeight: 'bold',
        marginTop: '5px',
        marginBottom: '15px',
      }}
    >
      {comment}
    </div>
  );
};

export default Failcomment;
