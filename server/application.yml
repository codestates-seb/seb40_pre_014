spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/stack?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    driver-class-name: org.mariadb.jdbc.Driver
    username: root
    password:
  jpa:
    open-in-view: false
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: create
  main:
    allow-circular-references: true
  jwt:
    #??? ?? ???????.
    secret:
  mvc:
    path match:
      matching-strategy: ant_path_matcher